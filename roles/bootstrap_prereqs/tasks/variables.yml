---
- name: Include OS family variables
  ansible.builtin.include_vars:
    file: "{{ role_path }}/vars/{{ ansible_os_family }}-family.yml"

- name: Include distribution version variables
  ansible.builtin.include_vars:
    file: "{{ role_path }}/vars/{{ ansible_distribution }}-{{ ansible_distribution_major_version }}.yml"